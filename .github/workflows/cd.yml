name: cd

on:
  push:
    branches: [main]

jobs:
  cd:
    name: cd
    runs-on: ubuntu-latest

    steps:  
      - name: Checkout code
        uses: actions/checkout@v5
      
      - name: Build the app
        run: ./scripts/buildprod.sh
